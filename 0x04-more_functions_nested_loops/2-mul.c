#include "main.h"

/**
 * mul - function that multiplies two integers.
 * @a: this is the character to be used
 * @b: this is the character to be used
 * Return: return 0 if success
 */

int mul(int a, int b)
{
	int multiply;

	multiply = a * b;
	return (multiply);
}
