#include <stdio.h>
/**
 * main - Printf the give words
 * Return: 0 if th code success. otherwiser no success
 */
int main(void)
{
        printf("with proper grammar, but the outcome is a piece of art,\n");
        return (0);
}
